name: Autodeploy

on:
  push:
    branches:
      - main
      
env:
  VPS_IP: ${{ secrets.VPS_IP }}
  VPS_USER: root
  
jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Connect to VPS
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.VPS_IP }}
        username: ${{ secrets.VPS_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          echo Pulling latest changes from Git
          cd /home/irvyn-azure/backend-api
          git pull
